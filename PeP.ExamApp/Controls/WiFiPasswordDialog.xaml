<Window x:Class="PeP.ExamApp.Controls.WiFiPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Connect to WiFi"
        Width="360"
        Height="220"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True">

    <Border Background="#1E293B" 
            BorderBrush="#3B82F6" 
            BorderThickness="2" 
            CornerRadius="12">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0">
                <TextBlock Text="🔐 Connect to WiFi" 
                           Foreground="White" 
                           FontSize="16" 
                           FontWeight="SemiBold"/>
                <TextBlock x:Name="NetworkNameText"
                           Text="Network Name" 
                           Foreground="#94A3B8" 
                           FontSize="12"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Password Input -->
            <StackPanel Grid.Row="1" Margin="0,20,0,0">
                <TextBlock Text="Password" 
                           Foreground="#94A3B8" 
                           FontSize="11"
                           Margin="0,0,0,6"/>
                <PasswordBox x:Name="PasswordInput"
                             Background="#0F172A"
                             Foreground="White"
                             BorderBrush="#334155"
                             BorderThickness="1"
                             Padding="12,10"
                             FontSize="14"/>
            </StackPanel>

            <!-- Error Message -->
            <TextBlock Grid.Row="2"
                       x:Name="ErrorText"
                       Text=""
                       Foreground="#EF4444"
                       FontSize="11"
                       Margin="0,8,0,0"
                       Visibility="Collapsed"/>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                <Button x:Name="CancelButton"
                        Content="Cancel"
                        Background="#374151"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,10"
                        FontSize="12"
                        Cursor="Hand"
                        Click="OnCancelClick"
                        Margin="0,0,10,0"/>
                <Button x:Name="ConnectButton"
                        Content="Connect"
                        Background="#3B82F6"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="20,10"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="OnConnectClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
